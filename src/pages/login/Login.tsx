import React from 'react'
import { Store } from 'redux'
import { iProps } from '../../interfaces/iComponentProps'

const Login = (props: iProps) => {
  return <div>Login</div>
}

Login.InitPropsData = (store: Store) => {

}

export { Login }